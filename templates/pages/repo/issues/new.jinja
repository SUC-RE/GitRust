{% extends "pages/repo/layout.jinja" %}
{% block title %}New Issue — {{ repo.owner_name }}/{{ repo.name }}{% endblock %}
{% block repo_content %}
<h2>New Issue</h2>
<form method="post" action="/{{ repo.owner_name }}/{{ repo.name }}/-/issues/new">
    <div class="form-group">
        <label for="title">Title</label>
        <input type="text" id="title" name="title" class="form-control" required autofocus>
    </div>
    <div class="form-group">
        <label for="description">Description (Markdown supported)</label>
        <textarea id="description" name="description" class="form-control" style="min-height:200px;"></textarea>
    </div>
    <button type="submit" class="btn btn-primary">Submit Issue</button>
</form>
{% endblock %}
